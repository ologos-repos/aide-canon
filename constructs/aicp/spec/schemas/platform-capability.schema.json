{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/ologos-repos/AICP/spec/schemas/platform-capability.schema.json",
  "title": "AICP Platform Capability Document",
  "description": "Served at /.well-known/aicp.json — describes an AICP-compliant platform's capabilities and enrollment endpoint.",
  "type": "object",
  "required": ["app_version", "platform_name", "profiles", "enrollment_url", "mcp_url_template"],
  "properties": {
    "app_version": {
      "type": "string",
      "description": "AICP specification version implemented (e.g., '0.1.0')"
    },
    "platform_name": {
      "type": "string",
      "description": "Human-readable platform name"
    },
    "profiles": {
      "type": "array",
      "items": {
        "type": "string",
        "description": "Profile identifier (standard: 'market', 'lifecycle', 'history', 'federation'; custom: 'x-*')"
      },
      "description": "Implemented AICP profiles"
    },
    "enrollment_url": {
      "type": "string",
      "format": "uri",
      "description": "URL to begin agent enrollment (POST)"
    },
    "mcp_url_template": {
      "type": "string",
      "description": "URL template for Card-scoped MCP endpoints. Use {card_id} as the placeholder."
    },
    "oauth_providers": {
      "type": "array",
      "items": {"type": "string"},
      "description": "Supported OAuth identity providers (e.g., 'github', 'google')"
    },
    "supported_classes": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["id", "name"],
        "properties": {
          "id": {
            "type": "string",
            "description": "Class identifier"
          },
          "name": {
            "type": "string",
            "description": "Human-readable class name"
          },
          "description": {
            "type": "string",
            "description": "What this work class covers"
          }
        }
      },
      "description": "Available work classes on this platform"
    },
    "contact": {
      "type": "object",
      "properties": {
        "email": {"type": "string", "format": "email"},
        "url": {"type": "string", "format": "uri"}
      },
      "description": "Platform contact information"
    },
    "federation": {
      "type": "object",
      "description": "Federation configuration. Required when 'federation' profile is declared in profiles array.",
      "required": ["signing_key_url", "federation_policy", "attestation_endpoint"],
      "properties": {
        "signing_key_url": {
          "type": "string",
          "format": "uri",
          "description": "URL to the platform's JWKS endpoint (/.well-known/jwks.json)"
        },
        "federation_policy": {
          "type": "string",
          "enum": ["open", "allowlist", "registry"],
          "description": "How this platform decides which attestation issuers to trust. 'open' = trust any verifiable issuer. 'allowlist' = only trust listed issuers. 'registry' = trust issuers in a shared registry."
        },
        "trusted_issuers": {
          "type": "array",
          "items": {
            "type": "object",
            "required": ["issuer", "trust_level"],
            "properties": {
              "issuer": {
                "type": "string",
                "format": "uri",
                "description": "Base URL of the trusted platform (must match the 'iss' claim in attestations)"
              },
              "trust_level": {
                "type": "string",
                "enum": ["full", "selective", "verify_only"],
                "description": "'full' = accept all claims. 'selective' = accept only claims in attribute_filter. 'verify_only' = accept for identity proof only, ignore metric claims."
              },
              "attribute_filter": {
                "type": "array",
                "items": {"type": "string"},
                "description": "Claim keys to accept. ['*'] means all. Only relevant when trust_level is 'selective'."
              },
              "notes": {
                "type": "string",
                "description": "Human-readable note about this trust relationship"
              }
            }
          },
          "description": "Explicitly trusted platforms. Required when federation_policy is 'allowlist'."
        },
        "registry_url": {
          "type": "string",
          "format": "uri",
          "description": "URL of the shared trust registry. Required when federation_policy is 'registry'."
        },
        "attestation_endpoint": {
          "type": "string",
          "description": "URL template for retrieving a Card's attestation. Use {card_id} as the placeholder."
        },
        "federation_contact": {
          "type": "string",
          "description": "Contact email or URL for federation partnership inquiries"
        }
      }
    }
  }
}
